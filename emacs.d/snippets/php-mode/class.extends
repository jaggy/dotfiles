# -*- mode: snippet -*-
# name: class extends
# key: cle
# --
<?php

namespace `(laravel/namespace)`;

class `(laravel/class-name buffer-file-name)` extends ${1:ParentClass}
{
    public function __construct()
    {
        $0
    }
}