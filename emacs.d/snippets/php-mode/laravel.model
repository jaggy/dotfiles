# -*- mode: snippet -*-
# name: laravel model
# key: lm
# --
<?php

namespace `(file-path-to-namespace)`;

class `(file-name-nondirectory (file-name-sans-extension (buffer-file-name)))` extends Model
{
    $0
}