# -*- mode: snippet -*-
# name: extended class
# key: cle
# --
<?php

namespace `(file-path-to-namespace)`;

class ${1:`(file-name-nondirectory (file-name-sans-extension (buffer-file-name)))`} extends ${2:ParentClass}
{
	/**
	 * Create a new $1 instance.
	 *
	 * @return $1
	 */
	public function __construct()
	{
		$0
	}
}