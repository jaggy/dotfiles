# -*- mode: snippet -*-
# name: test case
# key: tc
# --
<?php

namespace `(file-path-to-namespace)`;

use Tests\TestCase;
use Illuminate\Foundation\Testing\RefreshDatabase;

class `(php-class-name buffer-file-name)` extends TestCase
{
    use RefreshDatabase;

    $0
}
