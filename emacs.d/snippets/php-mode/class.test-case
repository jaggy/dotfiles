# -*- mode: snippet -*-
# name: test case
# key: testcase
# --
<?php

namespace `(laravel/namespace)`;

use Illuminate\Foundation\Testing\RefreshDatabase;
use Tests\TestCase;

class `(laravel/class-name buffer-file-name)` extends TestCase
{
    use RefreshDatabase;

    $0
}
